@include(variables.sm);
@include(templates.sm);

a { size: $size_base; }
h1 {
  font {{
    family: arial, helvetica, sans-serif;
    size: $size_title;
  }}
  
  padding {{
    left: 3px;
    top: 1px;
    bottom: 2px;
    right: 6px;
  }}
}

div {
  color: $mainColor;
  border: 1x solid $mainColor;
  padding: $padding;
}
