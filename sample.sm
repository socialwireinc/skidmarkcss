@include(variables.sm);
@include(templates.sm);

#page {
  margin: 0 auto;
  width: $page_width;
}

div {
  $width = 1024px;
  $height = $width * 3 / 4;
  &.surprise {
    $width = $width / 2;
    @@use box($width, $height, pink, red);
  }
  
  @@use box($width, $height, blue, black);
}
