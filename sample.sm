@@template box($width, $height) {
  width: $width;
  height: $height;
  
  &.doubled {
    width: $width * 2;
    height: $height * 2;
  }
}

div {
  @@use box(300px, 250px);
  
  &.special-case {
    @@use box(800px, 270px);
  }
}