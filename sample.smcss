@import url("test.css");

* { font-family: arial, helvetica, "some font", sans-serif; font-size: 12px; }
a:link, a:visited { color: #333399; font-weight: bold; text-decoration: none; }

ul {
  @include(sample-ul.smcss);
  &.test { color: red; }
  em { font-weight: bold; }
  li { color: green; }
}

p::before {
  line-height:20px;
}
